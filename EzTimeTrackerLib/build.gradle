apply plugin: 'java'
apply plugin: 'jacoco'

sourceSets {
    test {
        resources {
            srcDir 'src/test/resources'
        }
    }
}
dependencies {
    testCompile 'junit:junit:4.+'
    compile fileTree(dir: 'libs', include: ['*.jar'])
}